---
import { getEntry } from 'astro:content';
import Home from '../templates/Home.astro';

const entry = await getEntry('pages', 'index');
if (!entry) throw new Error('Missing pages/index.md content entry');
---

<Home entry={entry} />
